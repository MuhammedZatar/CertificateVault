<div class="bg-gray-50 min-h-screen flex items-center justify-center">
  <div class="text-center max-w-lg p-8 bg-white shadow-lg rounded-lg">
    <% if user_signed_in? %>
      <h1 class="text-4xl font-bold text-indigo-600">Welcome back to CertVault!</h1>
      <p class="text-lg text-gray-600 mt-4">
        Go to your <%= link_to "Dashboard", dashboard_path, class: "text-indigo-600 underline" %> to manage your certificates.
      </p>
    <% else %>
      <h1 class="text-4xl font-bold text-indigo-600">Welcome to CertVault!</h1>
      <p class="text-lg text-gray-600 mt-4">
        Securely manage and store your certificates with ease.
      </p>
      <div class="mt-8 flex justify-center space-x-4">
        <%= link_to "Sign In", new_user_session_path, class: "px-6 py-3 bg-indigo-600 text-white font-medium rounded-lg shadow hover:bg-indigo-700 focus:outline-none focus:ring focus:ring-indigo-400" %>
        <%= link_to "Sign Up", new_user_registration_path, class: "px-6 py-3 bg-gray-100 text-gray-700 font-medium rounded-lg shadow hover:bg-gray-200 focus:outline-none focus:ring focus:ring-gray-300" %>
      </div>
    <% end %>
  </div>
</div>
